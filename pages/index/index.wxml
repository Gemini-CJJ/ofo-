<!--pages/index/index.wxml-->
<view class='container'>
  <map longitude="{{longitude}}" latitude="{{latitude}}" id='ofo-map' scale='18' show-location controls='{{controls}}' bindcontroltap='bindcontroltap'></map>

</view>
